refactor(inclusion): add LibLogState, proofLengthRoot, storage variants

- includedRoot: add proofLengthRoot/proofLengthRootStorage and
  verifyInclusionStorage; verifyInclusion now uses proofLengthRoot.
  Add NatSpec for new primitives.
- Add LibLogState (algorithms/lib): verifyInclusion, verifyGrantInclusion,
  and storage variants that call verifyInclusionStorage (no full acc copy).
- Verifier: use LibLogState (using directive), add verifyGrantInclusion;
  keep verifyInclusion. Add tests for verifyGrantInclusion.
- Univocity: use LibLogState for grant inclusion; root branch uses memory
  log.verifyGrantInclusion, owner branch uses
  LibLogState.verifyGrantInclusionStorage(ownerLog, ...). Remove
  _leafCommitment (logic in LibLogState).
- includedRoot.t.sol: add harnesses and tests for proofLengthRoot,
  proofLengthRootStorage, verifyInclusionStorage (match verifyInclusion,
  wrong hash returns false).
